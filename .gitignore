# Секреты и переменные окружения
.env
.env.*

# Виртуальное окружение Python
.venv/
venv/
__pycache__/
*.pyc

# Данные (база, договоры, фото, бэкапы) — не должны попадать в git,
# живут в /data на сервере (или в DATA_DIR), не в репозитории
/data/
*.db
*.db-journal

# Логи
*.log

# Системное
.DS_Store
Thumbs.db
